{
  "name": "css-merge",
  "version": "0.0.1-alpha",
  "description": "A tool that merges rules from two css files.",
  "scripts": {
    "test": "node_modules/.bin/mocha tests/fixture_*.js",
    "watchtests": "node_modules/.bin/mocha -R list -w --recursive -G tests/fixture_*.js"
  },
  "main": "./index.js",
  "bin": {
    "cssmerge": "./cli.js"
  },
  "author": "Andrew Theken",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.6.0",
    "css": "^2.1.0",
    "lodash": "^3.0.0"
  },
  "precommit": [
    "test"
  ],
  "devDependencies": {
    "mocha": "^2.1.0",
    "pre-commit": "0.0.11",
    "which": "^1.0.8"
  }
}